CREATE TABLE COMENTARIO(
id_coment integer PRIMARY KEY AUTO_INCREMENT NOT NULL,
COMENTARIO TEXT NOT NULL,
CRIACAO DATETIME NOT NULL,
MOD_CRIACAO DATETIME NOT NULL,
id_fk integer not null,
CONSTRAINT id_fk FOREIGN KEY (id_fk) REFERENCES usuario(id)
)ENGINE=MyISAM;